"""
    Python needs `__init__.py` files because so that it recognizes this folder (/CodeCompass-Refactored) as a importable packages
    - So Relative imports work
    - & so that python3 -m yourpackage.run works
    - These can be left empty for the most part
"""